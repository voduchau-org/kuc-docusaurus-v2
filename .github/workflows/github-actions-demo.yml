name: Deployment Workflow A

on:
  push:
    branches:
      - test-co # Adjust this to your main branch

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # Deployment steps here
      - name: Sleep for 60s
        run: sleep 60

      - name: Trigger Workflow B
        uses: actions/workflow_run@v2
        with:
          workflow: Github Pages -> Algolia Crawler # Replace with the name of your b.yml workflow
          token: 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
